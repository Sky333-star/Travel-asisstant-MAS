# ---------------------------------------------------------------------------
# Wayfarer backend — runtime dependencies
# Pinned to compatible ranges; all packages are free/open-source.
# ---------------------------------------------------------------------------

# --- Web layer ---
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.9
sse-starlette>=2.1.0

# --- Config / validation ---
pydantic>=2.8.0,<3.0
pydantic-settings>=2.4.0,<3.0
python-dotenv>=1.0.1

# --- Agent orchestration ---
langgraph>=0.2.60,<0.7
langgraph-checkpoint-sqlite>=2.0.0,<3.0
langchain-core>=0.3.0,<0.4

# --- MCP (Model Context Protocol) ---
# Works with SDK 1.x (FastMCP) and 2.x (MCPServer) via
# mcp_servers/common/server_compat.py.
mcp>=1.9.0

# --- HTTP / data ---
httpx>=0.27.0,<0.29
tenacity>=8.5.0
aiosqlite>=0.20.0

# --- Observability (all free / OSS) ---
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
prometheus-client>=0.20.0
structlog>=24.4.0
langfuse>=2.50.0,<4.0
