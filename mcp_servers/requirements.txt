# ---------------------------------------------------------------------------
# Wayfarer MCP servers — runtime dependencies.
# Deliberately lean: each server is a standalone stdio process, so it only
# needs the MCP SDK plus an async HTTP client.
# ---------------------------------------------------------------------------
# Works with SDK 1.x (FastMCP) and 2.x (MCPServer) via
# mcp_servers/common/server_compat.py.
mcp>=1.9.0
httpx>=0.27.0,<0.29
pydantic>=2.8.0,<3.0
python-dotenv>=1.0.1
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
